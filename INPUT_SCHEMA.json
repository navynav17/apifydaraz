{
  "title": "Daraz Nepal HTTP Collector Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "brand": {
      "title": "Brand",
      "type": "string",
      "description": "Brand to search. Every Daraz search uses Brand + Category.",
      "editor": "textfield",
      "default": "Samsung"
    },
    "category": {
      "title": "Category",
      "type": "string",
      "description": "Category to search together with the brand.",
      "editor": "select",
      "enum": ["smartphone","tablet","laptop","desktop computer","printer","camera","smart tv","computer monitor","refrigerator","washing machine","air conditioner","dishwasher"],
      "default": "smartphone"
    },
    "maxSearchPages": {
      "title": "Maximum Search Pages",
      "type": "integer",
      "description": "Maximum Daraz search pages for the Brand + Category query.",
      "default": 100,
      "minimum": 1,
      "maximum": 100
    },
    "minPrice": {
      "title": "Minimum Price (NPR)",
      "type": "integer",
      "description": "Exclude products whose final PDP price is below this amount.",
      "default": 5000,
      "minimum": 0
    },
    "pdpConcurrency": {
      "title": "PDP HTTP Concurrency",
      "type": "integer",
      "description": "Number of product pages requested concurrently.",
      "default": 6,
      "minimum": 1,
      "maximum": 30
    },
    "detailDelayMs": {
      "title": "PDP Delay (ms)",
      "type": "integer",
      "description": "Delay between PDP requests.",
      "default": 100,
      "minimum": 0,
      "maximum": 10000
    },
    "maxRunSeconds": {
      "title": "Maximum Run Seconds",
      "type": "integer",
      "description": "Internal safety time budget.",
      "default": 900,
      "minimum": 60,
      "maximum": 3600
    }
  },
  "required": ["brand","category"]
}
