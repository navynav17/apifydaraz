{
  "title": "Daraz Nepal HTTP Collector Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "brand": {
      "title": "Brand",
      "type": "string",
      "description": "Optional brand. Used only when Strict Brand is enabled.",
      "editor": "textfield",
      "default": "Samsung"
    },
    "strictBrand": {
      "title": "Strict Brand Search",
      "type": "boolean",
      "description": "When false, discover the full category. When true, search Brand + Category only.",
      "default": false
    },
    "category": {
      "title": "Category",
      "type": "string",
      "description": "Select a suggested category or type any custom Daraz category/search term.",
      "editor": "select",
      "enumSuggestedValues": [
        "smartphone",
        "tablet",
        "laptop",
        "desktop computer",
        "printer",
        "camera",
        "smart tv",
        "computer monitor",
        "refrigerator",
        "washing machine",
        "air conditioner",
        "dishwasher"
      ],
      "enumTitles": [
        "Smartphone",
        "Tablet",
        "Laptop",
        "Desktop Computer",
        "Printer",
        "Camera",
        "Smart TV",
        "Computer Monitor",
        "Refrigerator",
        "Washing Machine",
        "Air Conditioner",
        "Dishwasher"
      ],
      "default": "smartphone"
    },
    "maxSearchPages": {
      "title": "Maximum Search Pages",
      "type": "integer",
      "description": "Maximum Daraz search pages. Daraz listing responses commonly expose about 40 products per page.",
      "default": 100,
      "minimum": 1,
      "maximum": 100
    },
    "minPrice": {
      "title": "Minimum Price (NPR)",
      "type": "integer",
      "description": "Exclude products whose listing price is below this amount.",
      "default": 5000,
      "minimum": 0
    },
    "pdpConcurrency": {
      "title": "PDP HTTP Concurrency",
      "type": "integer",
      "description": "Unused in the current HTTP search-only collector. PDP fetching is disabled.",
      "default": 6,
      "minimum": 1,
      "maximum": 30
    },
    "detailDelayMs": {
      "title": "PDP Delay (ms)",
      "type": "integer",
      "description": "Unused in the current HTTP search-only collector. PDP fetching is disabled.",
      "default": 100,
      "minimum": 0,
      "maximum": 10000
    },
    "maxRunSeconds": {
      "title": "Maximum Run Seconds",
      "type": "integer",
      "description": "Internal safety time budget.",
      "default": 900,
      "minimum": 60,
      "maximum": 3600
    }
  },
  "required": ["category"]
}
