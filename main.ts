import { Actor } from 'apify';
import { createClient } from '@supabase/supabase-js';

const BASE='https://www.daraz.com.np';
const MARKET='6a4f8822-e1bc-4e8b-be61-4d1a400f3c13';
const SUPABASE_URL=process.env.SUPABASE_URL||'https://foupthwcnnskqlzhoyep.supabase.co';
const KEY=process.env.SUPABASE_SERVICE_ROLE_KEY;
const CATEGORIES=['smartphone','tablet','laptop','desktop computer','printer','camera','smart tv','computer monitor','refrigerator','washing machine','air conditioner','dishwasher'];

type Item={title:string;url:string;itemId:string;price:number;image?:string;raw?:any};
type Product={title:string;url:string;itemId:string;price:number;image?:string;brand?:string;rating?:number;reviewCount?:number;specifications:Record<string,string>};
const clean=(v:any)=>String(v??'').replace(/\s+/g,' ').trim();
const num=(v:any)=>{const m=String(v??'').replace(/,/g,'').match(/\d+(?:\.\d+)?/);return m?Number(m[0]):undefined;};
const price=(v:any)=>{const m=clean(v).match(/(?:Rs\.?|NPR)?\s*[\d,]+(?:\.\d+)?/i);return m?num(m[0]):num(v);};
const normalize=(v:any)=>{let x=String(v??'').replace(/\\\//g,'/').replace(/\\u002F/gi,'/').replace(/&amp;/gi,'&').trim();if(x.startsWith('//'))x='https:'+x;try{const u=new URL(x,BASE);return u.hostname.endsWith('daraz.com.np')?`${BASE}${u.pathname}`:x;}catch{return x;}};
const idOf=(u:string)=>u.match(/(?:\/i|\/products\/[^?#]*?-i)(\d+)/i)?.[1]||u;
const isProduct=(u:string)=>/\/(?:i\d+(?:-s\d+)?|products\/[^?#]*-i\d+(?:-s\d+)?)\.html/i.test(u);
async function get(url:string){const c=new AbortController();const t=setTimeout(()=>c.abort(),25000);try{const r=await fetch(url,{signal:c.signal,redirect:'follow',headers:{'User-Agent':'Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 Chrome/131.0.0.0 Safari/537.36','Accept':'application/json,text/html,application/xhtml+xml;q=0.9,*/*;q=0.8','Accept-Language':'en-US,en;q=0.9','X-Requested-With':'XMLHttpRequest','Referer':BASE+'/','Cache-Control':'no-cache'}});return{status:r.status,type:r.headers.get('content-type')||'',text:await r.text()};}finally{clearTimeout(t);}}
function jsonRoot(text:string){try{return JSON.parse(text);}catch{return null;}}
function assignedJson(text:string,name:string){const marker=text.indexOf(name);if(marker<0)return null;const start=text.indexOf('{',marker+name.length);if(start<0)return null;let depth=0,inStr=false,esc=false;for(let i=start;i<text.length;i++){const ch=text[i];if(inStr){if(esc)esc=false;else if(ch==='\\')esc=true;else if(ch==='"')inStr=false;continue;}if(ch==='"'){inStr=true;continue;}if(ch==='{')depth++;else if(ch==='}'&&--depth===0){try{return JSON.parse(text.slice(start,i+1));}catch{return null;}}}return null;}
function rootsFrom(text:string):any[]{const roots:any[]=[];const j=jsonRoot(text);if(j)roots.push(j,j?.data,j?.result,j?.data?.result);for(const name of ['window.pageData','window.__pageData__','pageData']){const x=assignedJson(text,name);if(x)roots.push(x,x?.data,x?.result,x?.data?.result);}return roots.filter(Boolean);}
function itemsFrom(text:string):any[]{for(const r of rootsFrom(text)){const x=r?.mods?.listItems||r?.data?.mods?.listItems||r?.result?.mods?.listItems||r?.items||r?.listItems;if(Array.isArray(x))return x;}return[];}
function makeItem(x:any):Item|undefined{const u=normalize(x?.itemUrl||x?.productUrl||x?.url||'');const n=String(x?.itemId||x?.item_id||x?.productId||'').match(/\d{6,}/)?.[0];const url=isProduct(u)?u:n?`${BASE}/i${n}.html`:'';if(!url)return;return{title:clean(x?.name||x?.title||''),url,itemId:idOf(url),price:price(x?.priceShow??x?.price??x?.salePrice??x?.discountPrice)??0,image:clean(x?.image||x?.imageUrl||x?.images?.[0])||undefined,raw:x};}
function extractItems(text:string):Item[]{const out=new Map<string,Item>();for(const x of itemsFrom(text)){const p=makeItem(x);if(p&&!out.has(p.itemId))out.set(p.itemId,p);}return[...out.values()];}

const SPEC_KEY=/^(brand|brand\s*name|model|model\s*name|series|color|colour|color\s*family|ram|ram\s*memory|memory|storage|storage\s*capacity|rom|internal\s*storage|display|screen|screen\s*size|resolution|refresh\s*rate|processor|cpu|gpu|graphics|chipset|operating\s*system|os|camera|rear\s*camera|front\s*camera|battery|battery\s*capacity|network|sim|sim\s*type|connectivity|wifi|bluetooth|ports?|usb|hdmi|dimensions?|dimension|weight|capacity|power|power\s*consumption|voltage|warranty|warranty\s*period|condition|type|product\s*type|panel|panel\s*type|brightness|response\s*time|printer\s*type|print\s*speed|paper\s*size|lens|sensor|megapixel|zoom|video|refrigerant|energy\s*rating|wash\s*capacity|spin\s*speed|cooling\s*capacity|inverter|tonnage|door\s*type|installation\s*type)$/i;
const SKIP_KEY=/^(requestParams|request_params|itemId|item_id|productId|product_id|skuId|sku_id|sku|sellerId|seller_id|seller|sellerName|seller_name|itemUrl|productUrl|url|name|title|image|imageUrl|images|imageList|price|priceShow|salePrice|discountPrice|discount|originalPrice|currency|rating|ratingScore|ratings|review|reviews|reviewCount|reviewCountShow|shopId|shopName|brandId|brandName|categoryId|categoryName|categoryPath|tags|tracking|trackingInfo|analytics|query|searchTerm|search_term)$/i;
function specs(raw:any):Record<string,string>{const out:Record<string,string>={};const walk=(v:any)=>{if(v==null)return;if(Array.isArray(v)){for(const x of v)walk(x);return;}if(typeof v!=='object')return;for(const[k,val]of Object.entries(v)){if(val==null||SKIP_KEY.test(k))continue;if(typeof val==='string'||typeof val==='number'||typeof val==='boolean'){if(SPEC_KEY.test(clean(k))){const value=clean(val);if(value&&value.length<=300)out[clean(k)]=value;}continue;}walk(val);}};walk(raw);return out;}
function product(i:Item):Product{const r=i.raw||{};const s=specs(r);const brand=clean(r.brandName||r.brand||s.Brand||s['Brand Name'])||undefined;return{title:clean(i.title||r.name||r.title),url:i.url,itemId:i.itemId,price:price(r.priceShow??r.price??r.salePrice??r.discountPrice)??i.price,image:i.image,brand,rating:num(r.ratingScore||r.rating||r.ratings),reviewCount:num(r.reviewCount||r.review||r.reviews||r.reviewCountShow),specifications:s};}
async function save(p:Product,category:string,supabase:any){const{data:row,error}=await supabase.from('products').upsert({title:p.title,price:p.price,currency:'NPR',image:p.image||null,link:p.url,reviews:p.reviewCount??null,rating:p.rating??null,search_term:category,website:'Daraz Nepal',marketplace_id:MARKET,external_id:p.itemId,specifications:p.specifications},{onConflict:'marketplace_id,external_id'}).select('id').single();if(error||!row?.id)throw new Error(error?.message||'product upsert failed');const id=row.id;const{error:e2}=await supabase.from('price_history').insert({product_id:id,price:p.price,currency:'NPR',captured_at:new Date().toISOString()});if(e2)console.log(`PRICE HISTORY ERROR | ${p.itemId} | ${e2.message}`);const{error:e3}=await supabase.from('product_enrichment_queue').upsert({product_id:id,brand:p.brand||null,model:p.specifications.Model||p.specifications['Model Name']||null,product_type:category,parse_status:'needs_review',reason:'Daraz HTTP search collector',specifications:p.specifications,updated_at:new Date().toISOString()},{onConflict:'product_id'});if(e3)console.log(`ENRICHMENT ERROR | ${p.itemId} | ${e3.message}`);}

async function searchPage(query:string,page:number){const q=encodeURIComponent(query);const offset=(page-1)*40;const candidates=[
 `${BASE}/catalog/?q=${q}&_keyori=ss&from=input&page=${page}`,
 `${BASE}/catalog/?ajax=true&q=${q}&_keyori=ss&from=input&page=${page}`,
 `${BASE}/catalog/?ajax=true&q=${q}&_keyori=ss&from=input&page=1&totalResults=${offset}`,
 `${BASE}/catalog/?ajax=true&_keyori=ss&q=${q}&page=${page}&totalResults=${offset}`,
 `${BASE}/catalog/?q=${q}&_keyori=ss&from=input&totalResults=${offset}`
 ];
 for(const url of candidates){const r=await get(url);const its=extractItems(r.text);console.log(`FETCH | page=${page} | status=${r.status} | type=${r.type} | bytes=${Buffer.byteLength(r.text)} | structured=${its.length} | url=${url}`);if(its.length)return{r,its};}
 return{r:await get(candidates[0]),its:[]};}

async function main(){await Actor.init();if(!KEY)throw new Error('Missing SUPABASE_SERVICE_ROLE_KEY');const input:any=(await Actor.getInput())||{};const category=String(input.category||CATEGORIES[0]);const brand=clean(input.brand||'');const strictBrand=Boolean(input.strictBrand);const minPrice=Math.max(0,Number(input.minPrice??5000));const maxPages=Math.max(1,Math.min(100,Number(input.maxSearchPages||input.maxPages||100)));const query=clean(strictBrand&&brand?`${brand} ${category}`:category);const searchTerm=query;const supabase=createClient(SUPABASE_URL,KEY);const seen=new Set<string>();let saved=0,low=0,empty=0;console.log(`START | query="${query}" | mode=${strictBrand&&brand?'brand+category':'category-wide'} | maxPages=${maxPages} | price_min=${minPrice} | PDP=false`);
for(let page=1;page<=maxPages;page++){const{its}=await searchPage(query,page);const fresh=its.filter(x=>!seen.has(x.itemId));for(const x of fresh)seen.add(x.itemId);console.log(`SEARCH | page=${page}/${maxPages} | items=${its.length} | fresh=${fresh.length} | total=${seen.size}`);if(!its.length||!fresh.length){empty++;console.log(`EMPTY_PAGE | page=${page} | consecutive=${empty}`);if(empty>=5)break;}else empty=0;for(const x of fresh){const p=product(x);if(!p.price||p.price<minPrice){low++;continue;}try{await save(p,searchTerm,supabase);await Actor.pushData(p);saved++;}catch(e){console.log(`SAVE ERROR | ${p.itemId} | ${e instanceof Error?e.message:String(e)}`);}}}
console.log(`DONE | unique=${seen.size} | saved=${saved} | below_${minPrice}_or_missing_price=${low} | PDP_FETCHES=0`);await Actor.exit();}
main().catch(async e=>{console.error(e);try{await Actor.fail();}catch{}});
